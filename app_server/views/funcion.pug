extends layout

block content
    .row.mt-4
        h1.text-center 
            b Kohinor SB
        h3.text-center.text-black Funcionalidades 
        h4.mt-5.mb-5 Nuestra solución de software ERP brinda un método fácil y efectivo de administrar y gestionar tu empresa,  optimizando recursos y controlando procesos empresariales.
  
    .row 
        - var i = 0
        each element in functions 
            .col-md-4
                .card.m-5
                    .card-block.m-3
                        img.img-fluid.card-img-top(alt='Bootstrap Image Preview' src=`${element.img}`)
                        h4.card-title #{element.nombre}
                        h5.card-subtitle #{element.descripcion}
            - i += 1
            if i%3 == 0
                hr.bg-kohinor-2.p-4
            

                


